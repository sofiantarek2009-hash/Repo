// This component has been deprecated as the Survey CTA is now the main Hero interaction.
// Keeping file empty/placeholder to avoid build errors if referenced elsewhere, though usages were removed.
import React from 'react';
export const SurveyCTA: React.FC = () => null;